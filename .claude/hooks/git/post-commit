#!/bin/bash
# Git post-commit hook wrapper. Invokes the memory capture script.
SCRIPT_DIR="$(git rev-parse --show-toplevel)/.claude/hooks"
if [ -f "$SCRIPT_DIR/post-commit.sh" ]; then
    bash "$SCRIPT_DIR/post-commit.sh"
fi
exit 0
